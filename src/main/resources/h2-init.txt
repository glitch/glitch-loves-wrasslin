# H2's native full-text index initialization
CREATE ALIAS IF NOT EXISTS FT_INIT FOR "org.h2.fulltext.FullText.init";
CALL FT_INIT();

# Then you add it to a table
CREATE TABLE TEST(ID INT PRIMARY KEY, URL VARCHAR, TAGS VARCHAR);
INSERT INTO TEST VALUES(1, 'http://www.example.com', "example website tags");
# "tags" is our column name
CALL FT_CREATE_INDEX('PUBLIC', 'TEST', tags);

# Example search
SELECT * FROM FT_SEARCH('Hello', 0, 0);
# yields the query needed to fetch the result
QUERY: "PUBLIC"."TEST" WHERE "ID"=1


# The lucene version
CREATE ALIAS IF NOT EXISTS FTL_INIT FOR "org.h2.fulltext.FullTextLucene.init";
CALL FTL_INIT();

    CALL FTL_CREATE_INDEX('PUBLIC', 'PRODUCT', 'NAME,DESCRIPTION');

# Data
{
  "id": 0,
  "url": "https://www.instagram.com/p/DQHj8TeCRLM/",
  "notes": "instagram hand-fighting clip",
  "tags": [
    "hand fighting",
"clearing ties",
"clearing post",
"netural"
  ]
}